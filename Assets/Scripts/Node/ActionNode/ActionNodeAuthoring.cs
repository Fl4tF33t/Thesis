using UnityEngine;

//Abstract class for all action nodes to inherit from, adds the Node Authoring component
[RequireComponent(typeof(NodeAuthoring))]
public abstract class ActionNodeAuthoring : MonoBehaviour {
    
}
